I will update the `spi_bus_config` in `src/app/main/app_main.cpp` with the correct SPI bus pin configuration for the M5Stack Cardputer, based on the `todo/cardputer/info.md` document.

**1. Analyze `todo/cardputer/info.md`**

*   **ST7789V2 & RGB LED Table**:
    *   SCK (CLK): G36
    *   DAT (MOSI): G35
*   **microSD Socket Table** (Different SPI bus? Or same?):
    *   CLK: G40
    *   MOSI: G14
    *   MISO: G39
    *   CS: G12
*   The display (ST7789) and SD card seem to be on *different* sets of pins. The `SpiBusConfig` I am configuring is for the display (since it's next to `spi_display_config`).
*   The display table shows:
    *   SCK: G36
    *   DAT (MOSI): G35
    *   (No MISO listed for display, which is typical for ST7789 write-only/simplex mode).

**2. Modify** **`src/app/main/app_main.cpp`**

*   Update `SpiBusConfig spi_bus_config`:
    *   `.host_id`: `SPI2_HOST` (or `SPI3_HOST`? ESP32-S3 has SPI2 (FSPI) and SPI3 (HSPI). Usually SPI2 is preferred).
    *   `.sck_gpio`: `GPIO_NUM_36` (Correct).
    *   `.mosi_gpio`: `GPIO_NUM_35` (Correct).
    *   `.miso_gpio`: `GPIO_NUM_NC` (The current value `GPIO_NUM_32` is likely wrong or a leftover. The display doesn't list MISO. SD card uses G39. I should set this to NC (-1) if not used).
    *   `.max_transfer_sz`: Keep as is or adjust (usually `SOC_SPI_MAXIMUM_BUFFER_SIZE` or similar, 4096 is fine).

**Correction**: The current code in `app_main.cpp` has:
```cpp
SpiBusConfig spi_bus_config = {
  .host_id = SPI2_HOST,
  .sck_gpio = GPIO_NUM_36,
  .mosi_gpio = GPIO_NUM_35,
  .miso_gpio = GPIO_NUM_32, // <--- This needs check
  .max_transfer_sz = 4096,
};
```
Based on the doc, the display doesn't use MISO. The SD card uses G39 for MISO. If this bus is *only* for the display, MISO should be `GPIO_NUM_NC`. If it's shared, we need to check if G39 is valid. But the pins (36/35 vs 40/14) are totally different, so they are separate buses or the doc lists physical pins that map to different IOs.
Wait, ESP32-S3 has a pin mux. But usually board layouts use specific pins.
Display: 36(SCK), 35(MOSI).
SD: 40(CLK), 14(MOSI).
These are different. So the display bus is dedicated. I will set `miso_gpio` to `GPIO_NUM_NC`.

**Action:**
Update `app_main.cpp` `spi_bus_config`:
*   `miso_gpio = GPIO_NUM_NC`
*   Ensure `sck_gpio = 36` and `mosi_gpio = 35`.

Let's do this.