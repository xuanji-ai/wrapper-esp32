I will add a `Queue` class template to `wrapper-esp32/example/main/freertps.hpp` inside the `wrapper` namespace.

This class will encapsulate FreeRTOS queue functionality, providing a type-safe interface for:
- Creating a queue (`xQueueCreate`)
- Sending items (`xQueueSend`, `xQueueSendFromISR`)
- Receiving items (`xQueueReceive`, `xQueueReceiveFromISR`)
- Peeking (`xQueuePeek`)
- Checking status (`uxQueueMessagesWaiting`)
- Resource management (RAII-style destructor with `vQueueDelete`)

The implementation will be added at the end of the file, before the closing brace of the `wrapper` namespace.