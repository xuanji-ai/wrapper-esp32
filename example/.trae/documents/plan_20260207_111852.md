I will optimize the `DisplayBase`, `I2cDisplay`, `SpiDisplay`, and `DsiDisplay` classes to use `bool` as the return type for their operation methods, simplifying error handling for the upper layers.

1. **Modify** **`src/wrapper/display.hpp`**:

   * Update `DisplayBase` methods (`IoTxParam`, `IoTxColor`, `Reset`, `Init`, `DrawBitmap`, etc.) to return `bool`.

   * Update `I2cDisplay` and `SpiDisplay` `Init`/`Deinit` and private initialization methods to return `bool`.

2. **Modify** **`src/wrapper/display.cpp`**:

   * Update the implementation of `I2cDisplay` and `SpiDisplay` methods to match the new `bool` return type.

   * Ensure errors are logged internally before returning `false`.

3. **Modify** **`src/wrapper/display-dsi.hpp`**:

   * Update `DsiBus` and `DsiDisplay` private initialization methods (`InitIo`, `InitPanel`) to return `bool` for consistency.

   * (Note: `Init` and `Deinit` already return `bool` in the header, but I will ensure consistency).

4. **Modify** **`src/wrapper/display-dsi.cpp`**:

   * Fix the logic in `DsiDisplay::Init` where it was returning `ret` (which could be an error code) instead of `false` on error.

   * Update `InitIo` and `InitPanel` implementations to return `bool`.

5. **Modify** **`src/board/m5stack/core-s3.cpp`**:

   * Update `M5StackCoreS3::InitDevice` to check for `false` return from `ili9341.Init` instead of `ESP_OK`.

6. **Verification**:

   * `src/board/m5stack/tab5.cpp` uses `DsiDisplay` methods but ignores return values, so it will remain compatible.

