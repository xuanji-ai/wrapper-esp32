I have read both files and identified the issue.

**In `wrapper/audio.hpp`:**
*   `Microphone` class (lines 49-84) has `bool Write(const void *data, size_t size);` (line 77) but **NO** `Read` method.
*   `SpeakerCodec` class (lines 86-135) has `bool Read(void *data, size_t size);` (line 127) but **NO** `Write` method?
    *   Wait, let's look closer at `SpeakerCodec` in `audio.hpp`:
        *   Line 127: `bool Read(void *data, size_t size);`
        *   Line 130: `template<typename T> bool Read(...)`
        *   It seems `SpeakerCodec` has `Read` instead of `Write` in the header! This is wrong for a Speaker.
    *   And `MicrophoneCodec` (lines 137-186) has `bool Read(void *data, size_t size);` (line 178) which is correct.

**The Error:**
1.  `Microphone` class in `audio.hpp` has `Write` instead of `Read`.
2.  `SpeakerCodec` class in `audio.hpp` has `Read` instead of `Write`.

**The Fix:**
1.  In `wrapper/audio.hpp`:
    *   Change `Microphone::Write` to `Microphone::Read`.
    *   Change `SpeakerCodec::Read` to `SpeakerCodec::Write`.

**Wait, double check `SpeakerCodec` in `audio.hpp`**:
Lines 86-135.
Line 127: `bool Read(void *data, size_t size);` -> This should be `Write`.
Line 130: `template<typename T> bool Read(...)` -> This should be `Write`.

**Wait, double check `Microphone` in `audio.hpp`**:
Lines 49-84.
Line 77: `bool Write(const void *data, size_t size);` -> This should be `Read`.
Line 80: `template<typename T> bool Write(...)` -> This should be `Read`.

**Wait, double check `Speaker` in `audio.hpp`**:
Lines 12-47.
Line 40: `bool Write(...)`. Correct.

**Wait, double check `MicrophoneCodec` in `audio.hpp`**:
Lines 137-186.
Line 178: `bool Read(...)`. Correct.

**Summary of Mismatches:**
*   `Microphone` (no codec) has `Write` but needs `Read`.
*   `SpeakerCodec` has `Read` but needs `Write`.

I will fix `wrapper/audio.hpp`. The `wrapper/audio.cpp` implementation is actually correct (it implements `Microphone::Read` and `SpeakerCodec::Write`), hence the "no declaration matches" error.

Let's apply the fix.