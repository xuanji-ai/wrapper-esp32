I will refactor the `Nvs` and `Event` classes in `src/wrapper/soc.hpp` and `src/wrapper/soc.cpp` to change the return type of their operational methods from `esp_err_t` to `bool`.

**1. Modify** **`src/wrapper/soc.hpp`**

* Update `Nvs` class: Change return types of `Init`, `Erase`, `Commit`, `OpenNamespace`, `EraseKey`, `SetValue`, `GetValue`, `SetString`, `GetString` to `bool`.

* Update `Event` class: Change return types of `CreateLoopDefault`, `DeleteLoopDefault`, `CreateLoop`, `DeleteLoop`, `RunLoop`, `Register`, `Unregister`, `Post`, `PostFromIsr`, `Dump` to `bool`.

**2. Modify** **`src/wrapper/soc.cpp`**

* Implement the boolean return logic for all modified methods in `Nvs` and `Event`.

* Update `Nvs` implementation:

  * Return `true` if `esp_err_t` was `ESP_OK` (or specific success cases like `ESP_ERR_NVS_NOT_FOUND` for `GetValue` might be considered failure or handled differently - usually `GetValue` returning false on not found is expected behavior for a boolean API).

  * Return `false` on error.

  * Keep existing logging.

* Update `Event` implementation:

  * Return `true` if `esp_err_t` was `ESP_OK`.

  * Return `false` on error.

  * Keep existing logging.

**3. Verification**

* Since there are no usages found in the provided example code, I will just verify that the changes compile.

**Refactoring Details:**

* **Nvs::Init**: Return `true` on success.

* **Nvs::OpenNamespace**: Return `true` on success.

* **Nvs::Erase**: Return `true` on success.

* **Nvs::Commit**: Return `true` on success.

* **Nvs::EraseKey**: Return `true` on success.

* **Nvs::SetValue**: Return `true` on success.

* **Nvs::GetValue**: Return `true` on success. If key not found, return `false`.

* **Nvs::SetString**: Return `true` on success.

* **Nvs::GetString**: Return `true` on success. If key not found, return `false`.

* **Event::CreateLoopDefault**: Return `true` on success.

* **Event::DeleteLoopDefault**: Return `true` on success.

* **Event::CreateLoop**: Return `true` on success.

* **Event::DeleteLoop**: Return `true` on success.

* **Event::RunLoop**: Return `true` on success.

* **Event::Register**: Return `true` on success.

* **Event::Unregister**: Return `true` on success.

* **Event::Post**: Return `true` on success.

* **Event::PostFromIsr**: Return `true` on success.

* **Event::Dump**: Return `true` on success.

