I will encapsulate the remaining FreeRTOS APIs included in `freertps.hpp` (`semphr.h` and `event_groups.h`) by adding the following classes to the `wrapper` namespace:

1.  **Semaphore Wrappers**:
    *   **`Semaphore` (Base Class)**: Encapsulates `SemaphoreHandle_t` and common operations (`Take`, `Give`, `Delete`).
    *   **`BinarySemaphore`**: Wrapper for `xSemaphoreCreateBinary`.
    *   **`CountingSemaphore`**: Wrapper for `xSemaphoreCreateCounting`.
    *   **`Mutex`**: Wrapper for `xSemaphoreCreateMutex`.
    *   **`RecursiveMutex`**: Wrapper for `xSemaphoreCreateRecursiveMutex` with recursive take/give operations.

2.  **Event Group Wrapper**:
    *   **`EventGroup`**: Encapsulates `EventGroupHandle_t` and provides methods for:
        *   `WaitBits`, `SetBits`, `ClearBits`, `GetBits`, `Sync`.
        *   ISR variants (`SetBitsFromISR`, `ClearBitsFromISR`, `GetBitsFromISR`).

These classes will be added to `freertps.hpp` after the `Queue` class.