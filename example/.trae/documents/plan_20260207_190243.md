I need to implement `I2sChanPdmTxConfig` struct and add `ConfigureTxChannel(I2sChanPdmTxConfig&)` to `I2sBus` class.

**1. `wrapper/i2s.hpp`**
*   Add `struct I2sChanPdmTxConfig` inheriting from `i2s_pdm_tx_config_t`.
*   Add method declaration `bool ConfigureTxChannel(I2sChanPdmTxConfig& chan_config);` to `I2sBus`.

**2. `wrapper/i2s.cpp`**
*   Implement `I2sBus::ConfigureTxChannel(I2sChanPdmTxConfig& chan_config)` using `i2s_channel_init_pdm_tx_mode`.

**Note:** `i2s_pdm_tx_config_t` typically includes clock config (`sample_rate_hz`, `clk_src`, `mclk_multiple`, `up_sample_mode`? or similar PDM specific) and slot config (`data_bit_width`, `slot_mode`, etc.).
I will look up the likely structure or assume similar to Rx but with TX specific fields (like `up_sample_mode` instead of `dn_sample_mode` if applicable, or check if it uses `i2s_pdm_tx_clk_config_t` which might have `up_sample_mode`).
Based on previous user input for Rx, `i2s_pdm_rx_clk_config_t` had `dn_sample_mode`. `i2s_pdm_tx_clk_config_t` likely has `up_sample_mode` (type `i2s_pdm_usr_t`?).
I will assume standard parameters for now and let the user correct or I'll check compilation.
Actually, I'll search for `i2s_pdm_tx_config_t` definition if possible, but I can't search easily without tools.
However, I can follow the pattern of `I2sChanPdmRxConfig` but adapted for TX.
Typical PDM TX config:
- Clock: `sample_rate_hz`, `clk_src`, `mclk_multiple`, `up_sample_mode` (usually `I2S_PDM_USR_x`).
- Slot: `data_bit_width`, `slot_mode` (mono/stereo), `slot_mask`, `line_mode`?
- GPIO: `clk`, `dout`.

I will create the struct with these assumptions.