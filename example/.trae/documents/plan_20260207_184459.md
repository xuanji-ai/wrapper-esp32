I will enable PDM Microphone support in the wrapper and generate the configuration in `app_main.cpp`.

**1. Update `wrapper/i2s.hpp`**
*   Include `<driver/i2s_pdm.h>`.
*   Add `struct I2sChanPdmRxConfig` inheriting from `i2s_pdm_rx_config_t`.
*   Add `ConfigureRxChannel(I2sChanPdmRxConfig& chan_config)` to `I2sBus` class.

**2. Update `wrapper/i2s.cpp`**
*   Implement `ConfigureRxChannel(I2sChanPdmRxConfig& chan_config)` using `i2s_channel_init_pdm_rx_mode`.

**3. Update `main/app_main.cpp`**
*   Define `I2sBusConfig i2s_mic_bus_cfg` for `I2S_NUM_1` (Rx only).
*   Define `I2sChanPdmRxConfig i2s_mic_chan_cfg` with Cardputer pins (CLK=43, DIN=46) and 16kHz sample rate (as per info2).

This will fully support the requested microphone configuration using the project's wrapper architecture.