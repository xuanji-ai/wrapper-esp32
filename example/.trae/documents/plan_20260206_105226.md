I will complete the `Queue` class in `freertps.hpp` by adding the missing FreeRTOS Queue APIs.

I will add the following methods to the `Queue` class template:

1.  `SendToFront`: Wraps `xQueueSendToFront`.
2.  `SendToFrontFromISR`: Wraps `xQueueSendToFrontFromISR`.
3.  `Overwrite`: Wraps `xQueueOverwrite`.
4.  `OverwriteFromISR`: Wraps `xQueueOverwriteFromISR`.
5.  `PeekFromISR`: Wraps `xQueuePeekFromISR`.
6.  `SpacesAvailable`: Wraps `uxQueueSpacesAvailable`.
7.  `MessagesWaitingFromISR`: Wraps `uxQueueMessagesWaitingFromISR`.
8.  `IsQueueEmptyFromISR`: Wraps `xQueueIsQueueEmptyFromISR`.
9.  `IsQueueFullFromISR`: Wraps `xQueueIsQueueFullFromISR`.

These additions will provide a more complete C++ wrapper for the FreeRTOS Queue API.