I will update the `spi_display_config` in `src/app/main/app_main.cpp` with the correct pin configuration for the M5Stack Cardputer, based on the information extracted from `todo/cardputer/info.md`.

**1. Analyze `todo/cardputer/info.md`**

*   **ST7789V2 & RGB LED Table**:
    *   DISP_BL: G38
    *   RST: G33
    *   RS (DC): G34
    *   DAT (MOSI): G35
    *   SCK (CLK): G36
    *   CS: G37
*   **Specifications from Web Search (context)**:
    *   Screen: ST7789V2 @ 1.14 Inch, 240 x 135px
    *   (Usually ST7789 uses SPI Mode 0 or 3, max clock often 40MHz or higher).

**2. Modify** **`src/app/main/app_main.cpp`** (Actually path is `example/main/app_main.cpp`)

*   Update `SpiDisplayConfig spi_display_config`:
    *   `.cs_gpio = 37` (from G37)
    *   `.dc_gpio = 34` (from RS G34)
    *   `.clock_speed_hz`: Increase to `40000000` (40MHz) which is typical for ST7789, or keep at 20MHz if conservative. I'll use 40MHz as it's standard for these panels.
    *   `.spi_mode = 0` (Standard for ST7789)
    *   `.reset_gpio`: The config struct in `SpiDisplayConfig` likely has a reset pin field? Checking `wrapper/display.hpp` might be needed, but `SpiDisplayConfig` usually inherits from `esp_lcd_panel_io_spi_config_t` which doesn't have reset. Reset is usually passed to `esp_lcd_panel_dev_config_t`.
    *   Wait, `SpiDisplayConfig` in `wrapper/display.hpp` combines IO and Panel config.
    *   I should check `wrapper/display.hpp` to see the full struct definition to ensure I fill in all fields correctly, especially `reset_gpio` if it exists there.

**Checking `wrapper/display.hpp` (mental check or tool)**:
*   I read `wrapper/display.hpp` in a previous turn (Task 1).
*   `SpiDisplayConfig` struct likely has:
    *   `spi_config` (cs, dc, clk_speed, mode...)
    *   `panel_dev_config` (reset_gpio, color_space, bits_per_pixel...)
*   Wait, looking at `app_main.cpp` current content:
    ```cpp
    SpiDisplayConfig spi_display_config = {
      .cs_gpio = 15,
      .dc_gpio = 2,
      // ...
    };
    ```
    This looks like a flat struct. I'll double check `wrapper/display.hpp` to be precise.

**Refined Plan:**
1.  Read `wrapper/display.hpp` to verify `SpiDisplayConfig` structure.
2.  Update `app_main.cpp` with Cardputer pins:
    *   CS: 37
    *   DC: 34
    *   RST: 33 (Need to see where this goes)
    *   BL: 38 (Need to see where this goes, usually separate control or via panel config if supported, but typically it's a separate GPIO or LEDC).
    *   MOSI: 35, SCK: 36 (These are SPI bus config, passed to `SpiBus`, not `SpiDisplayConfig` directly, unless `SpiDisplay` initializes the bus too? Usually `SpiBus` is initialized separately).
    *   Checking `app_main.cpp` again: It includes `wrapper/spi.hpp`. It likely needs to initialize `SpiBus` first.
    *   The snippet in `app_main.cpp` only shows `SpiDisplayConfig`. I need to setup the `SpiBus` too.
    *   The user only asked to "fill correct parameters into `spi_display_config`". I will focus on that, but I should also probably add the `SpiBus` setup code if it's missing or incorrect, to make it work.
    *   Current `app_main.cpp` snippet for Cardputer is very empty inside `board_init`.

**Action:**
1.  Read `wrapper/display.hpp` and `wrapper/spi.hpp`.
2.  Update `app_main.cpp` to:
    *   Define `SpiBusConfig` with MOSI=35, MISO=NC (-1), SCLK=36.
    *   Define `SpiDisplayConfig` with CS=37, DC=34, RST=33.
    *   Initialize `SpiBus` and `SpiDisplay` (using `ST7789` driver if available, or generic `SpiDisplay` with custom init if needed, but `esp_lcd_new_panel_st7789` exists in IDF).

I will assume the user just wants the `spi_display_config` updated as per the prompt, but providing a working init sequence is better.

**Pin Mapping Summary for Code:**
*   SCK: 36
*   MOSI: 35
*   MISO: -1 (Not used for display typically)
*   CS: 37
*   DC: 34
*   RST: 33
*   BL: 38

I will proceed to read the header first to ensure struct correctness.