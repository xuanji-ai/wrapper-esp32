I will refactor the `LvglPort` class to return `bool` instead of `esp_err_t` for its operational methods.

**1. Modify** **`src/wrapper/lvgl.hpp`**

* Change return types of `Init`, `Deinit`, `AddDisplay`, `AddDisplayDsi`, and `AddTouch` from `esp_err_t` to `bool`.

**2. Modify** **`src/wrapper/lvgl.cpp`**

* Implement the boolean return logic for the modified methods.

* Log errors internally using the logger and return `false` on failure, `true` on success.

**3. Modify** **`src/board/m5stack/core-s3.cpp`**

* Update `M5StackCoreS3::InitMiddleware` to check boolean return values.

**4. Modify** **`src/board/m5stack/tab5.cpp`**

* Update `M5StackTab5::Init` to check boolean return values for `lvgl_port` methods.

  ```cpp
  if (!lvgl_port.Init(lvgl_port_cfg)) {
      return false;
  }
  if (!lvgl_port.AddDisplayDsi(dsi_display, lvgl_display_cfg, lvgl_dsi_cfg)) {
      return false;
  }
  if (!lvgl_port.AddTouch(gt911_touch, lvgl_touch_cfg)) {
      return false;
  }
  ```

