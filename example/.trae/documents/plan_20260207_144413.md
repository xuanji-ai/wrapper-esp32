I will refactor `DsiBus` and `DsiDisplay` in `src/wrapper/display-dsi.hpp` and `src/wrapper/display-dsi.cpp` to return `bool` instead of `esp_err_t` from their operational methods (`Init`, `Deinit`), consistent with other wrappers. The upper layer should only care about success or failure.

**1. Modify** **`src/wrapper/display-dsi.hpp`**

*   Update `DsiBus::Init` and `DsiBus::Deinit` return types to `bool`.
*   Update `DsiDisplay::Init` and `DsiDisplay::Deinit` return types to `bool`.
*   Update private helpers `InitIo` and `InitPanel` to return `bool` as well.

**2. Modify** **`src/wrapper/display-dsi.cpp`**

*   Implement the boolean return logic.
*   Return `true` on success (`ESP_OK`) and `false` on failure.
*   Log errors internally (which is already done mostly, but ensure all error paths log).

**3. Verification**

*   I need to check if any other files depend on `DsiBus::Init` or `DsiDisplay::Init` returning `esp_err_t`.
    *   `src/board/m5stack/tab5.cpp`: I recently refactored this to use `Ili9881c` (which uses `DsiDisplay`). `Ili9881c::Init` calls `DsiDisplay::Init`.
    *   `src/device/ili9881c.cpp`: `Ili9881c::Init` calls `DsiDisplay::Init`.
        ```cpp
        return DsiDisplay::Init(...) == ESP_OK;
        ```
        I will need to update this to simply return `DsiDisplay::Init(...)`.
    *   `src/device/ili9881c.hpp`: `Ili9881c::Init` already returns `bool`, so the signature matches the user's desire. I just need to update the implementation to not check for `== ESP_OK`.

**Plan Details:**

1.  **Refactor `display-dsi.hpp`**: Change return types.
2.  **Refactor `display-dsi.cpp`**: Update implementations.
3.  **Refactor `device/ili9881c.cpp`**: Update `Init` implementation to handle the boolean return from `DsiDisplay::Init`.

Let's proceed.