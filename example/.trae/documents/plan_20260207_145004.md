I will extract the "Specifications" and "Pin Mapping" tables from the provided PowerHub documentation context and save them to `todo/powerhub/info.md`.

**1. Create/Update** **`todo/powerhub/info.md`**

*   **Specifications Section**:
    *   Extract the `Specification | Parameter` table.
    *   Include SoC, MCU, Flash, PSRAM, Wi-Fi, RTC, Interface specs, etc.
*   **Pin Mapping Section**:
    *   Extract the `RS485 & CAN` pin map.
    *   Extract other pin mappings if present in the text (the provided snippet cuts off after "CAN", but I will extract what is available: "RS485 & CAN ESP32-S3 G8 G18 G17 G39 G40...").
    *   Wait, the text snippet provided for "Pin Mapping" is truncated: `RS485 & CAN ESP32-S3 G8 G18 G17 G39 G40 RS485 MCU_485_TXD MCU_485_DIR MCU_485_RXD CAN ...`.
    *   I will use the `WebFetch` tool to get the full content of the "Pin Mapping" section from the URL `https://docs.m5stack.com/zh_CN/core/PowerHub` to ensure I have the complete table data, as the context might be incomplete.

**Revised Plan:**

1.  **Fetch Full Content**: Use `WebFetch` to get the complete markdown/text from the M5Stack PowerHub documentation page to ensure I have the full pin map.
2.  **Extract Data**: Parse the fetched content for the "Specifications" (规格参数) and "Pin Mapping" (管脚映射) tables.
3.  **Write File**: Create `todo/powerhub/info.md` with the formatted tables.

Let's do this.