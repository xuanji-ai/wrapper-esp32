I will refactor the `LdoRegulator` class to return `bool` instead of `esp_err_t` for its operational methods, allowing the upper layer to simply check for success or failure.

**1. Modify** **`src/wrapper/ldo.hpp`**

* Change return types of `Init`, `Deinit`, and `AdjustVoltage` from `esp_err_t` to `bool`.

**2. Modify** **`src/wrapper/ldo.cpp`**

* Update `Init`:

  * Return `true` on success (`ESP_OK`).

  * Return `false` on failure (logging the specific error internally).

* Update `Deinit`:

  * Return `true` on success.

  * Return `false` on failure.

* Update `AdjustVoltage`:

  * Return `true` on success.

  * Return `false` on failure.

**3. Modify** **`src/board/m5stack/tab5.cpp`**

* Update the initialization of `dsi_phy_ldo` to check the boolean return value:

  ```cpp
  if (!dsi_phy_ldo.Init(dsi_phy_ldo_cfg)) {
      return false;
  }
  ```

