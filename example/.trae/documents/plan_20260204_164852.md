Refactor `display.hpp` and `dsi.hpp` to integrate DSI configurations into `DsiLcdConfig`.

1.  **Modify `src/wrapper/dsi.hpp`**:
    *   Remove `struct DsiDbiIoConfig` and `struct DpiPanelConfig`.
    *   Update `DsiBus::CreateDbiIo` signature to accept `const esp_lcd_dbi_io_config_t &config` instead of `DsiDbiIoConfig`.

2.  **Modify `src/wrapper/dsi.cpp`**:
    *   Update `DsiBus::CreateDbiIo` implementation to match the new signature.

3.  **Modify `src/wrapper/display.hpp`**:
    *   Update `struct DsiLcdConfig`:
        *   Replace `DsiDbiIoConfig` member with `esp_lcd_dbi_io_config_t`.
        *   Replace `DpiPanelConfig` member with `esp_lcd_dpi_panel_config_t`.
        *   Update constructor to accept all parameters required for DBI IO and DPI Panel configuration directly.
        *   Implement the constructor to initialize `dbi_io_config`, `dpi_panel_config`, and `panel_config` members explicitly.
        *   Order constructor parameters: Required DPI parameters first, followed by optional parameters with defaults.

This refactoring centralizes the DSI LCD configuration within `DsiLcdConfig`, giving it full ownership of the configuration structures as requested.