I need to update `AudioCodec` and `AudioNone` in `src/wrapper/audio.hpp` and `src/wrapper/audio.cpp` to store `I2sBus` as a **reference** instead of a pointer, following the updated project rule.

**1. Update `wrapper/audio.hpp`**

*   **`AudioCodec` class**:
    *   Change `I2sBus *i2s_bus_` member to `I2sBus &i2s_bus_`.
    *   Update constructor to accept `I2sBus&` and initialize the reference member.
    *   Currently `AudioCodec` constructor is `AudioCodec(Logger &logger)`.
    *   The rule says "For objects needing underlying wrapper injection... pass reference in constructor and store reference".
    *   However, `AudioCodec` currently initializes `I2sBus` via `Init(I2sBus &i2m_bus)`.
    *   If I must store a reference, I *must* pass it in the constructor. References cannot be rebound or initialized later.
    *   So I need to change the constructor signature of `AudioCodec` to `AudioCodec(Logger &logger, I2sBus &i2s_bus)`.
    *   And remove `Init(I2sBus &i2m_bus)` or change it to `Init()`.
    *   Wait, `Init` does other things (audio codec interface init). I should keep `Init()` but remove the bus argument if it's in the constructor. Or keep `Init()` for the heavy lifting.

*   **`AudioNone` class**:
    *   Same change: `I2sBus *i2s_bus_` -> `I2sBus &i2s_bus_`.
    *   Update constructor: `AudioNone(Logger &logger, I2sBus &i2s_bus)`.
    *   Remove `Init(I2sBus &i2s_bus)` or change signature.

**2. Update `wrapper/audio.cpp`**

*   Update constructors to initialize the reference members.
*   Update methods to use `i2s_bus_` directly (dot operator `.` instead of arrow `->`) and remove null checks for the bus (references are assumed valid).
*   Update `Init` methods.

**3. Impact Analysis**

*   This is a breaking change for `AudioCodec` instantiation.
*   I need to check where `AudioCodec` is instantiated.
    *   It's used in `board/m5stack/core-s3.hpp` (implied, `M5StackCoreS3` likely has an `AudioCodec` member).
    *   It's used in `AudioNone` (newly added, likely used in `app_main.cpp` later).
    *   I should check `src/board/m5stack/core-s3.hpp` and other board files to update their constructors if they instantiate `AudioCodec`.
    *   Since `AudioCodec` is now requiring `I2sBus` in constructor, the board class (which owns both) needs to initialize them in the correct order in its initializer list.

**Revised Plan:**

1.  **Search for `AudioCodec` usage**: Check board files.
2.  **Modify `AudioCodec`**:
    *   Constructor: `AudioCodec(Logger&, I2sBus&)`
    *   Member: `I2sBus& i2s_bus_`
    *   Remove `Init(I2sBus&)` argument.
3.  **Modify `AudioNone`**:
    *   Constructor: `AudioNone(Logger&, I2sBus&)`
    *   Member: `I2sBus& i2s_bus_`
    *   Remove `Init(I2sBus&)` argument.
4.  **Update Board Files**:
    *   `src/board/m5stack/core-s3.hpp` / `.cpp`
    *   `src/board/m5stack/tab5.hpp` / `.cpp` (if used)
    *   Check for others.

Let's start by searching for usages.